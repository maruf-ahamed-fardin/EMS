# Supply-chain guard (see docs/architecture.md, "Dependencies"). A package must have been public
# for a week before it can be installed, and install scripts never run.
min-release-age=7
ignore-scripts=true
save-exact=true
engine-strict=true
