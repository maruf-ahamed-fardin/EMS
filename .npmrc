# Don't run dependencies' install scripts, a common supply-chain attack vector.
# Native packages here ship prebuilt binaries through optional dependencies, so nothing needs them.
# `npm run <script>` still works; only lifecycle and pre/post scripts are skipped.
ignore-scripts=true
